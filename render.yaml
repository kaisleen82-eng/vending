services:
  - type: worker
    name: vending-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: VENDING_BOT_TOKEN
        sync: false
      - key: VENDING_ADMIN_IDS
        sync: false
